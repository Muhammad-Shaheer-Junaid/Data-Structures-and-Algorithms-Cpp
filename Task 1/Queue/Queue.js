let queue = [];  // Creating a queue

// Enqueue (Adding to the end)
queue.push(10);
queue.push(20);
queue.push(30);
console.log(queue);  // Output: [10, 20, 30]

console.log(queue.shift());  // Removes 10 (Output: 10)
console.log(queue.shift());  // Removes 20 (Output: 20)